# 🚀 دليل الاستضافة على Render

## المزايا
✅ **مجاني تماماً** (مع خطة مدفوعة اختيارية)
✅ **سهل جداً** — لا تحتاج لمعرفة تقنية عميقة
✅ **رابط https** آمن تلقائياً
✅ **تدعم قاعدة البيانات المحلية** (SQLite)
✅ **سريع جداً** على الهواتف

---

## الخطوة 1: إنشاء حساب على Render

1. اذهب إلى [render.com](https://render.com)
2. اضغط **Sign Up** (سجل باستخدام GitHub أو Email)
3. أكمّل التسجيل

---

## الخطوة 2: رفع الكود على GitHub

### أ) إنشاء Repository على GitHub

1. اذهب إلى [github.com/new](https://github.com/new)
2. أعط Repository اسم: `nashmi-cafe`
3. اختر **Public**
4. اضغط **Create repository**

### ب) رفع الملفات

في Terminal/PowerShell:

```bash
cd nashmi_cafe
git init
git add .
git commit -m "Initial commit - Nashmi Cafe System"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/nashmi-cafe.git
git push -u origin main
```

**استبدل `YOUR_USERNAME` باسم حسابك على GitHub**

---

## الخطوة 3: الاستضافة على Render

### 1. اذهب إلى Render Dashboard

- اضغط على حسابك في الأعلى اليمين
- اختر **Dashboard**

### 2. إنشاء Web Service جديد

- اضغط **New +**
- اختر **Web Service**

### 3. ربط Repository

- اختر **GitHub**
- ابحث عن `nashmi-cafe`
- اختره واضغط **Connect**

### 4. الإعدادات

| الحقل | القيمة |
|---|---|
| **Name** | `nashmi-cafe` |
| **Environment** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `gunicorn app:app` |
| **Instance Type** | `Free` |

### 5. اضغط **Create Web Service**

**⏳ الانتظار: 2-5 دقائق حتى تكتمل الاستضافة**

---

## ✅ النتيجة

ستحصل على رابط مثل:
```
https://nashmi-cafe.onrender.com
```

- **لوحة الإدارة:** https://nashmi-cafe.onrender.com/admin
- **منيو الزبائن:** https://nashmi-cafe.onrender.com/menu

---

## 📱 QR Code للمنيو

يوجد ملف `nashmi_menu_qr.png` يحتوي على QR Code
- اطبعه وضعه على الطاولات
- الزبائن يمسحونه بالهاتف مباشرة

---

## 🔄 تحديث التطبيق

عندما تعدّل على الكود:

```bash
cd nashmi_cafe
git add .
git commit -m "تحديثات جديدة"
git push origin main
```

**Render سيحدّث التطبيق تلقائياً بعد دقيقة**

---

## ⚠️ ملاحظات مهمة

### البيانات المحفوظة

قاعدة البيانات (nashmi_cafe.db) **تُحفظ على خادم Render**
- الطلبات والمبيعات محفوظة
- الذمم محفوظة
- المخزون محفوظ

### الصور

صور الأصناف التي ترفعها:
- تُحفظ على الخادم
- تظهر تلقائياً في المنيو
- **ملاحظة:** في النسخة المجانية قد تُحذف الملفات عند إعادة تشغيل الخادم
- **الحل:** ارفع الصور مرة واحدة فقط واترك التطبيق يعمل

---

## 🔐 الأمان

### كلمة المرور الافتراضية
- كلمة المرور: `admin123`
- **غيّرها فوراً** من الإعدادات

### الوصول
- أي شخص يعرف الرابط يستطيع دخول المنيو ✓ (هذا مقصود)
- لوحة الإدارة **بدون حماية** حالياً
- **النصيحة:** استخدم VPN أو اجعل الرابط سري للإدارة

---

## 📊 الرابط الدائم

```
https://nashmi-cafe.onrender.com
```

هذا الرابط **دائم** طالما الحساب نشط

---

## 💰 التكلفة

- **النسخة المجانية:** لا توجد تكلفة ✓
- **الحد الأقصى:** 0.5 جيجابايت RAM
- **الوقت:** خادم نشط 24/7
- **إذا أردت:** يمكنك الترقية لـ paid plan لمزيد من الموارد

---

## 🆘 المشاكل الشائعة

### ❌ الخطأ: "Build failed"
✅ تأكد أن `Procfile` موجود في المشروع
✅ تأكد أن `requirements.txt` صحيح
✅ اضغط **Redeployment** من Render

### ❌ الرابط بطيء جداً
✅ هذا طبيعي في الخطة المجانية
✅ الطلب الأول يستغرق 10-20 ثانية
✅ الطلبات اللاحقة أسرع

### ❌ الصور لا تظهر
✅ قد تكون محذوفة بسبب إعادة التشغيل
✅ أعد رفع الصور من لوحة الإدارة

---

## ✨ النتيجة النهائية

**لديك الآن:**
- ✅ نظام كامل على الإنترنت
- ✅ رابط آمن https
- ✅ QR Code للزبائن
- ✅ منيو يعمل على الهواتف
- ✅ لوحة إدارة كاملة

**للبدء الفعلي:**
1. فك الضغط والرفع على GitHub
2. ربط مع Render (5 دقائق)
3. اطبع QR Code
4. ابدأ العمل!

---

*نشمي كافيه على الإنترنت — بكل بساطة! ☕*
